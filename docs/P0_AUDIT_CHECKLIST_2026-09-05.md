# P0 independent exact-head audit checklist — 2026-09-05

The final reviewer must answer every item from current GitHub evidence:

## Git identity

- exact branch head;
- exact parent chain back to mining head `480c7524d3f9195281ed9dec9ce248da661999dd`;
- exact changed-file list;
- no frozen history rewrite.

## Inventory

- unfamiliar empty-slot appearance cannot publish occupied/full;
- official run requires positive 0/28 proof;
- floor remains 0.8;
- UNKNOWN never means not-full;
- post-click progress requires exactly +1 on a strictly newer frame.

## Resource/startup

- threshold remains 0.12;
- six landmarks remain configured;
- quorum remains 5/6;
- all three zones remain required;
- unsupported/ambiguous/foreign scene exposes zero targets;
- no open-loop camera ladder remains executable;
- software normalization uses world pixels only;
- transformed targets remain hover-proven before clicking;
- final READY uses a strictly newer clean frame.

## Input

- PREP carries no mining/navigation/banking authority;
- exact HWND and SHA are bound;
- one attempt dispatches no more than one click;
- click is never success;
- foreground, cursor, root ownership, geometry, and DPI are rechecked immediately before input;
- FULL at 28 produces no 29th click;
- navigation/banking remain disabled.

## Evidence

- retained real frames are not relabeled or exported;
- synthetic transform tests are labeled synthetic;
- first Windows run is read-only;
- failed PREP runs remain preserved as failures;
- no release-ready claim is made from experimental evidence.

Any failed item is a no-go for another live mining command.